import SwiftUI

struct HomeView: View {
    var body: some View {
        VStack {
            Spacer()
            Text("Hoş geldiniz")
                .font(.largeTitle)
            Spacer()
        }
    }
}

#Preview {
    HomeView()
}
